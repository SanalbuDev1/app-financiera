package com.finanzas.personales.finanzas.auth.domain.model;

/**
 * Enumeración que representa los roles posibles de un usuario en el sistema.
 * Forma parte del modelo de dominio puro — sin dependencias de frameworks.
 */
public enum UserRole {
    ADMIN,
    USER
}
