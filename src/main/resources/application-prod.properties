spring.application.name=finanzas

# =============================================
# BASE DE DATOS — Azure PostgreSQL Flexible Server (R2DBC)
# Tier Burstable B1ms — gratis 12 meses con cuenta free
# Se sobreescriben con variables de entorno en Azure Container Apps
# =============================================
spring.r2dbc.url=${SPRING_R2DBC_URL}
spring.r2dbc.username=${SPRING_R2DBC_USERNAME}
spring.r2dbc.password=${SPRING_R2DBC_PASSWORD}
spring.r2dbc.pool.initial-size=1
spring.r2dbc.pool.max-size=5
spring.r2dbc.pool.max-idle-time=5m
spring.r2dbc.pool.max-life-time=15m

# =============================================
# JWT — secret DEBE venir de variable de entorno
# =============================================
app.jwt.secret=${APP_JWT_SECRET}
app.jwt.expiration-ms=86400000

# =============================================
# CORS — dominio del frontend en producción
# =============================================
app.cors.allowed-origins=${APP_CORS_ALLOWED_ORIGINS:https://tu-frontend.azurestaticapps.net}

# =============================================
# WEBFLUX
# =============================================
spring.webflux.base-path=/
server.port=9000
server.address=0.0.0.0

# =============================================
# SPRINGDOC — Deshabilitado en producción
# =============================================
springdoc.api-docs.enabled=false
springdoc.swagger-ui.enabled=false

# =============================================
# LOGGING — menos verbose en producción
# =============================================
logging.level.root=WARN
logging.level.com.finanzas.personales=INFO
